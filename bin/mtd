#!/usr/bin/env node
const meow = require('meow')
const cli = require('./cli')
const createDownload = require('../index').createDownload

cli(createDownload, meow())
  .subscribe(x => console.log('Download Completed!'))
